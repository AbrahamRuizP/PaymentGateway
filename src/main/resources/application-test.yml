spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/payment_gateway_test
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

logging:
  level:
    root: WARN