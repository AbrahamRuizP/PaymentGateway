spring:

  datasource:
    url: jdbc:postgresql://localhost:5433/payment_gateway
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

logging:
  level:
    root: WARN